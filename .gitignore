# build output
dist/
.astro/

# dependencies
node_modules/

# local caches and artefacts
scripts/body/.cache/
test-results/
playwright-report/
shots/
*.log
.DS_Store

# the essay PDF is archived locally only (the Substack post is canonical)
docs/source/*.pdf

# simulation (sim/): environment, caches, raw run outputs, and papers kept for reading (never commit PDFs)
sim/.venv/
__pycache__/
.pytest_cache/
sim/results/*/raw/
sim/private/
